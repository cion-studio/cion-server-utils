{
  "name": "cion-server-utils",
  "version": "1.1.1",
  "description": "Various utilities and types used in Cion Studio projects",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p . && cp package.json dist && cp README.md dist",
    "deploy": "yarn version && npm run build && cd dist && npm publish",
    "test": "ts-node ./tests/server.ts"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^16.11.6",
    "eslint": "^7.30.0",
    "express": "^4.17.1",
    "typescript": "^4.3.5"
  }
}
